export = (UI: UIType): { [key: string]: string | undefined } => ({
    // 'window.activeBorder': UI.border, // Border color for the active (focused) window.
    // 'window.inactiveBorder': UI.none, // Border color for the inactive (unfocused) windows.
});
